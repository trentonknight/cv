{% import 'macros.html.j2' as m %}
{# Set the page variable for sidebar highlighting #}
{% set page = "exp" %}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Experience | {{ personal.name }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/terminal.css@0.7.4/dist/terminal.min.css" />
    <link rel="stylesheet" href="style.css" />
</head>
<body class="terminal">
    <div class="container page-wrapper">
        
        {% include 'sidebar.html.j2' %}

        <main>
            <h2>Professional Experience</h2>
            {# Corrected loop to point to experience.main #}
            {% for job in experience.main %}
            <div class="card">
                <div class="company-header">
                    <img src="assets/{{ job.image.split('/')[-1] }}" 
                         alt="{{ job.company }}" 
                         class="company-logo">
                    <h3>{{ m.link(job.url, job.company) }}</h3>
                </div>
                <p><strong>{{ job.role }}</strong> | {{ job.dates }}</p>
                <ul>
                    {% for bullet in job.bullets %}
                    <li>{{ bullet }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% endfor %}

            <footer style="margin-top: 40px; padding-top: 20px; border-top: 1px solid #e1e1e1;">
            </footer>
        </main>
    </div>
</body>
</html>
